# Optional: point the browser client at a separate Socket.IO server.
# Leave blank in production when the frontend and Socket.IO server are served
# from the same Render web service.
VITE_SOCKET_SERVER_URL=

# Optional: custom Socket.IO path. Defaults to /socket.io.
VITE_SOCKET_PATH=/socket.io
